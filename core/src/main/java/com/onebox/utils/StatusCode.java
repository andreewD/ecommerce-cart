package com.onebox.utils;

public enum StatusCode {
    SUCCESS,
    ERROR,
    PRODUCT_NOT_FOUND,
    PRODUCT_STOCK_NOT_ENOUGH,
    CART_NOT_FOUND,
    EMPTY_CART,
    PRODUCT_NOT_FOUND_IN_CART,
    INVALID_QUANTITY,
    INVALID_PRICE,
}
